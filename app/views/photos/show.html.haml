%h1= @photo.title
%p= @photo.description
%p= @photo.user.email

= link_to "Edit", edit_photo_path
= link_to "Delete", photo_path, method: :delete, data: { confirm: "Are you sure?" }
= link_to "Back", root_path
